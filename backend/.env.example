# --- Configurações do Protheus ---
PROTHEUS_REST_URL=https://rodolltda195384.protheus.cloudtotvs.com.br:10707/rest
PROTHEUS_USER=admin
PROTHEUS_PASSWORD=

# --- Configurações da IA (Ollama / OVMS / Gemini) ---
OLLAMA_URL=http://ollama:11434
# OLLAMA_MODEL=llama3

# Gemini
GEMINI_API_KEY=sua_chave_gemini_aqui
GEMINI_MODEL=gemini-2.5-flash

# JWT Auth
JWT_SECRET=copilot-protheus-secret-key-change-me
JWT_EXPIRY_SECONDS=3600

# Database Postgres + pgvector
DATABASE_URL=postgresql+psycopg2://postgres:password@postgres_db:5432/copilot_protheus

# Driver do banco Protheus: "oracle" para TOTVS Cloud (DBACCESS Oracle) | "mssql" para SQL Server
DB_DRIVER=oracle

JWT_SECRET=copilot-protheus-dev-secret-change-me
JWT_EXPIRY_SECONDS=3600
CORS_ORIGIN=*

# --- Cloudflare R2 (Armazenamento de Arquivos) ---
R2_ENDPOINT_URL=https://[account_id].r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=copilot-knowledge

# --- Hetzner Cloud (Infraestrutura) ---
HETZNER_API_TOKEN=

# --- Cloudflare API (DNS / Pages / Cache) ---
CLOUDFLARE_ZONE_ID=
CLOUDFLARE_API_TOKEN=
